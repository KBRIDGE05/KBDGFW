(()=>{const menu=document.getElementById("menuBtn"),mobile=document.getElementById("mobileNav");menu&&mobile&&(menu.addEventListener("click",()=>{const open=mobile.classList.toggle("open");menu.setAttribute("aria-expanded",String(open)),mobile.setAttribute("aria-hidden",String(!open))}),mobile.querySelectorAll("a").forEach(a=>a.addEventListener("click",()=>{mobile.classList.remove("open"),menu.setAttribute("aria-expanded","false"),mobile.setAttribute("aria-hidden","true")}))),document.querySelectorAll(".faq-item button").forEach(btn=>btn.addEventListener("click",()=>{const item=btn.closest(".faq-item");item.classList.toggle("open"),btn.setAttribute("aria-expanded",String(item.classList.contains("open")))})),window.KBPage={val:id=>(document.getElementById(id)?.value||"").trim()||"-",bytes:n=>n<1024?n+" B":n<1048576?(n/1024).toFixed(1)+" KB":(n/1048576).toFixed(1)+" MB",to64:file=>new Promise((resolve,reject)=>{const r=new FileReader;r.onload=()=>resolve(String(r.result).split(",")[1]||""),r.onerror=reject,r.readAsDataURL(file)}),fileManager({inputId:inputId,zoneId:zoneId,listId:listId,maxFiles:maxFiles=6,maxBytes:maxBytes=15728640}){const input=document.getElementById(inputId),zone=document.getElementById(zoneId),list=document.getElementById(listId);let files=[];const valid=f=>f.type.startsWith("image/")||"application/pdf"===f.type||f.name.toLowerCase().endsWith(".pdf");function render(){list.innerHTML=files.map((f,i)=>`<div class="file-item"><span>${f.name} · ${window.KBPage.bytes(f.size)}</span><button type="button" data-index="${i}">삭제</button></div>`).join(""),list.querySelectorAll("button").forEach(b=>b.addEventListener("click",()=>{files.splice(Number(b.dataset.index),1),render()}))}function add(next){const merged=[...files,...Array.from(next).filter(valid)].slice(0,maxFiles);merged.reduce((s,f)=>s+f.size,0)>maxBytes?alert(`첨부파일 합계는 ${Math.round(maxBytes/1048576)}MB를 넘을 수 없습니다.`):(files=merged,render())}return input?.addEventListener("change",e=>{add(e.target.files),e.target.value=""}),["dragenter","dragover"].forEach(type=>zone?.addEventListener(type,e=>{e.preventDefault(),zone.classList.add("drag")})),["dragleave","drop"].forEach(type=>zone?.addEventListener(type,e=>{e.preventDefault(),zone.classList.remove("drag")})),zone?.addEventListener("drop",e=>add(e.dataTransfer.files)),()=>files},async send(payload,statusId,button,successId){const status=document.getElementById(statusId),old=button.textContent;button.disabled=!0,button.textContent="접수 중...",status.textContent="문의 내용을 접수하고 있습니다.",status.style.color="";try{await window.KBridgeMail.send(payload),status.textContent="문의가 정상 접수되었습니다.",document.getElementById(successId)?.showModal()}catch(err){console.error(err),status.textContent="접수 중 오류가 발생했습니다. 잠시 후 다시 시도하거나 이메일로 문의해 주세요.",status.style.color="#c33"}finally{button.disabled=!1,button.textContent=old}}}})();